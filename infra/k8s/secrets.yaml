# Template — actual values should be injected via sealed-secrets or external-secrets
apiVersion: v1
kind: Secret
metadata:
  name: subscriptio-secrets
  namespace: subscriptio
type: Opaque
stringData:
  database-url: "jdbc:postgresql://postgres:5432/subscriptio"
  analytics-database-url: "postgresql+asyncpg://subscriptio:changeme@postgres:5432/subscriptio"
  jwt-secret: "change-me-in-production-must-be-at-least-256-bits"
  stripe-api-key: "sk_test_placeholder"
  stripe-webhook-secret: "whsec_placeholder"
